spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:haiapi}
    username: ${DB_USER:haiapi}
    password: ${DB_PASSWORD:haiapi123}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      password: ${REDIS_PASSWORD:}

logging:
  level:
    root: WARN
    com.haiapi: INFO
  file:
    name: logs/hai-api.log
